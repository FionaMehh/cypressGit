function showMessage(message){
    document.getElementById('message').textContent = message;
}

function changePercentoff(percentage) {
    document.getElementById('percent-of').textContent = percentage + "% OFF";
}